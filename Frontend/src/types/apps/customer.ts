export interface Customer {
    name_customer: string;     // Tên khách
    datetime: string;          // Ngày tạo
    ID_customer: string;       // ID khách hàng
    assistant: string;         // Trợ lý
    channel: string;           // Kênh marketing
    tag: string;               // Tags
    total_spend: number;       // Tổng chi tiêu
    phone_number: string;      // Số điện thoại
    address: string;
}